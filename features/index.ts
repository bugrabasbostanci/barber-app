// Feature modules export file
// Note: These are hook modules, not components, so no lazy loading needed

// Re-export barber profile (our newly refactored module)
export * from './barber-profile'

// Other features can be imported directly when needed to avoid naming conflicts